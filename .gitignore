# Local paths / credentials-ish machine configuration
config/local_paths.json
config/akerminne_local.json

# Raw / large data: never commit
data/raw/*
!data/raw/README.md

# ATL transaction exports are local/private experiment inputs; never commit
ATL_AkerSync_*.csv

# Derived data and built HTML are reproducible
data/derived/
dist/

# Whitebox / temp
work/
*.tif
*.gpkg
*.zip

# Python
__pycache__/
*.pyc
.venv/
qa/input_manifest.json
